{
	"title": "add journal types",
	"description": "add journal types data for send to server only IDs",
	"type": "array",
	"uniqueItems": true,
	"items": {
		"type": "object",
		"description": "type of counter",
		"properties": {
			"id": {
				"type": "number",
				"description": "type ID"
			},
			"name": {
				"type": "string",
				"description": "type name",
				"maxLength": 64,
				"minLength": 3,
				"pattern": "[A-Za-z0-9_-]+"
			}
		},
		"required": ["id", "name"]
	}
}
