rabbit:
  retryCount: 1
  user:
    exchange: user
    routingKey: add
    durable: true
    exclusive: false
    autoDelete: false
  status:
    exchange: status
    routingKey: update
    durable: true
    exclusive: false
    autoDelete: false
  websocket:
    exchange: websocket
    routingKey: send
    durable: true
    exclusive: false
    autoDelete: false
